@model WebApplication.Models.IndexViewModel

@{
    Layout = null;
    ViewBag.Title = "Flight Delays";
}

Search flight delay data:
<p/>

@using (Html.BeginForm("Search", "Home"))
{ 
    @Html.Label("From:")
    @Html.TextBox("From")
    @Html.Label("To:")
    @Html.TextBox("To")
    <p/>

    @Html.Label("Carrier:")
    @Html.TextBox("Carrier")
    <p/>

    <input type="submit" value="Search" />
}
